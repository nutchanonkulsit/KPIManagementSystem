{
  "name": "KPIManagementSystem",
  "private": true,
  "scripts": {
    "start:backend": "cd backend && npm install && npm run start",
    "start:frontend": "cd frontend && npm install && ng serve",
    "migrate": "cd backend && npm run db:migrate",
    "migrate:undo": "cd backend && npm run db:migrate:undo",
    "migrate:undo:all": "cd backend && npm run db:migrate:undo:all",
    "seed": "cd backend && npm run seed",
    "seed:undo": "cd backend && npm run seed:undo",
    "db:setup": "npm run migrate && npm run seed",
    "dev": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "docker:compose": "docker-compose up --build",
    "docker:db": "docker-compose run backend sh -c 'npm run db:migrate && npm run seed'",
    "docker": "npm run docker:compose && npm run docker:db"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
